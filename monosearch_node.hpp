#include <vector>
#include <list>

/* Get MonoData and search breakable points from it. */
/* return the best in broken. */
class MonoSearchNode {
public:
    static std::vector<MonoSearchNode*> work;
    
};
